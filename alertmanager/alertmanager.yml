# alertmanager/alertmanager.yml

route:
  receiver: "default"              
  group_by: ["alertname"]           
  group_wait: 30s                   
  group_interval: 5m                
  repeat_interval: 1h               

receivers:
  - name: "default"
    # En prod: 
    # slack_configs:
    #   - api_url: "https://hooks.slack.com/services/..."
    #     channel: "#alerts"
    # email_configs:
    #   - to: "admin@example.com"